- Walk throuh the entire codebase, list out all the available functions in all the essential .py files and the features related to the functions.
- Problem: currenlty the the bot is showing 60% of winning rate as per eod dashboard report, however overall it apprears a loss making engine
- Expectations: Need to identify and report why the bot is not making profit, and report complete solution
- provide complete report in paste ready markdown format

# Code Audit report 

You are a coding auditor AI tasked with analyzing a modular Python trading engine. 
Your responsibilities are:

1. **Codebase Walkthrough**
   - Traverse the entire repository.
   - For each essential `.py` file, list all available functions.
   - Document the purpose and features tied to each function.
   - Present findings in a structured, hierarchical format.

2. **Performance Diagnosis**
   - Current state: The bot shows ~60% win rate in the EOD dashboard report.
   - Problem: Despite the win rate, the engine is overall loss-making.
   - Expectation: Identify and explain why profitability is negative.
   - Analyze risk/reward imbalance, stop-loss vs target logic, survivability gaps, and trade attribution.

3. **Solution Report**
   - Provide a complete solution to address profitability issues.
   - Recommend refinements in modules (scalp, trend, reversal, breakout detection, order management).
   - Suggest governance improvements (conflict handling, attribution clarity, survivability scaling).
   - Ensure all recommendations are empirically testable and auditable.

4. **Output Format**
   - Deliver the final report in **paste-ready Markdown**.
   - Use clear section headings: *Codebase Functions*, *Diagnosis*, *Root Causes*, *Proposed Solutions*, *Action Checklist*.
   - Include bullet points, tables, and examples where relevant.
   - Ensure the report is comprehensive, structured, and ready for direct use in documentation or dashboards.

Constraints:
- Do not modify code autonomously; only report and recommend.
- Maintain strict governance boundaries: evidence must be tied to dashboard outputs and replay validation.
- Ensure transparency and reproducibility in all findings.


# Prompt :

# Prompt :

You are a coding agent with full visibility of the trading engine codebase.  
Your task is to autonomously recompile and refactor all strategic functions  
into a unified regime-adaptive strategy engine by making best use of available strategic functons and indicators. The final goal is to convert the engine from a loss‑making system into a profit‑making one.

---

## Scope
1. Walk through all strategy-related modules:
   - execution.py
   - entry_logic.py
   - reversal_detector.py
   - failed_breakout_detector.py
   - zone_detector.py
   - pulse_module.py

2. Recompile functions into a modular scoring engine that adapts to:
   - Pivot relationships with previous pivots
   - Day type classification (TRENDING, RANGE, GAP, BALANCE, NEUTRAL)
   - CPR width and status
   - Daily pivot width
   - Opening bias
   - Indicator configurations (ATR, RSI, CCI, ADX, EMA stretch, Supertrend, VWAP)
   - Zone Detector outputs (demand/supply zones, persistence across sessions)
   - Pulse Module outputs (tick_rate, burst_flag, drift, suppression reasons)

3. Ensure strategies are robust across market conditions by dynamically weighting  
   pivots, CPR, bias, zone context, and pulse signals.

4. Output a complete paste-ready Markdown report with:
   - Function map
   - Refactored strategy design
   - Context–Strategy Matrix (including zone + pulse integration)
   - Replay validation plan
   - Dashboard attribution requirements

5. Governance boundaries:
   - Do not deploy changes silently
   - Provide evidence via replay and dashboard metrics
   - Ensure transparency and reproducibility

---

## Scope of Refactor
- Cover all strategic modules listed above.
- Recompile functions into a regime‑adaptive scoring engine that dynamically weights:
  - Pivot relationships (previous vs current pivots, breakout/acceptance/failed breakout).
  - Day type classification (TRENDING, RANGE, GAP, BALANCE, NEUTRAL).
  - CPR width and status (narrow/wide).
  - Daily pivot width (ATR‑scaled).
  - Opening bias (gap up/down, oscillator relief).
  - Zone Detector outputs (demand/supply zones, persistence).
  - Pulse Module outputs (tick_rate, burst_flag, drift, suppression reasons).
  - Indicator configurations (ATR, RSI, CCI, ADX, EMA stretch, Supertrend, VWAP).

---

## Deliverables
- Paste‑ready Markdown report containing:
  - Function map (all refactored functions and their purpose).
  - Context–Strategy Matrix (rows = contexts like day type, CPR, pivots, zones, pulse; columns = scalp, trend, reversal, exit).
  - Refactored strategy design (how modules interact under different regimes).
  - Replay validation plan (sessions to run, metrics to compare).
  - Dashboard attribution requirements (new tags, W/L splits, survivability metrics).

---

## Validation Requirements
- Must run `run_offline_replay()` on recent sessions to prove improvements.
- Must log attribution for every suppression, entry, and exit reason.
- Must show dashboard evidence: win rate, avg win/loss size, profit factor, TG reach %, zone‑based W/L splits.

---

## Governance Boundaries
- No silent deployment — all changes reported first.
- Transparency: every refactor tied to replay and dashboard metrics.
- Reproducibility: outputs must be auditable and empirically validated.

---

# 📊 Context–Strategy Matrix Scaffold (Two Paths: Scalp & Trend)

| **Context** | **Scalp Module** | **Trend Module** | **Reversal Module** | **Exit Logic** |
|-------------|------------------|------------------|----------------------|----------------|
| **Day Type** (TRENDING / RANGE / GAP / BALANCE / NEUTRAL) | Entry suppression in RANGE/NEUTRAL; opportunistic bursts in TRENDING | Trend entries favored in TRENDING; must hold through momentum until option price peak | Reversal trades allowed in RANGE/NEUTRAL; blocked in TRENDING | Exit suppression adaptive to day type (longer holds in TRENDING) |
| **CPR Status** (Narrow / Wide) | Narrow CPR → scalp bursts; Wide CPR → suppression | Narrow CPR → trend TG widened (2.5–3×ATR); Wide CPR → trend entries suppressed | Reversal overrides when CPR narrow + oscillator extreme | Exit trail widened when CPR narrow |
| **Pivot Relationships** (Prev pivot acceptance / breakout / failed breakout) | Scalp dip/rally signals at pivots; avoid failed breakouts | Trend entries aligned with breakout acceptance; run through trend until option price peak | Failed breakout detection triggers reversal entries | Exit tightening when pivot rejection occurs |
| **Daily Pivot Width** (ATR-scaled) | PT/SL scaled to pivot width | TG scaled wider when pivot width narrow; SL tightened | Reversal strength scoring higher with wide pivots | Exit guardrails adjusted to pivot width |
| **Opening Bias** (Gap up/down, oscillator relief) | Scalp gating aligned with gap bias | Trend entries aligned with opening bias; must hold through gap momentum | Reversal overrides when gap + oscillator extreme | Exit suppression when opening bias persists |
| **Zone Detector** (Demand/Supply zones, persistence) | Scalp suppression near strong zones | Trend entries aligned with zone breakouts; must run through zone momentum until peak | Reversal triggers at zone rejections | Exit tightening when zone retest occurs |
| **Pulse Module** (tick_rate, burst_flag, drift) | Scalp entry allowed only if tick_rate ≥ burst_threshold | Trend entry quality adjusted by drift signals; momentum confirmation via pulse | Reversal confirmation when pulse shows exhaustion | Exit attribution tags: pulse suppression, burst fade |
| **Indicators** (ATR, RSI, CCI, ADX, EMA stretch, Supertrend, VWAP) | ATR scaling for PT/SL; oscillator gating | Trend scoring weights: ADX, EMA stretch, ST alignment; must hold until option price peak when ADX high | Reversal detection: EMA stretch + RSI/CCI extremes | Exit triggers: oscillator exit, DTS trail, momentum exhaustion |

---

## 🎯 Expectations

- **Two Paths**:  
  1. **Scalp Path** → Opportunistic bursts, tightly governed by tick_rate and suppression logic.  
  2. **Trend Path** → Momentum‑driven, designed to run through the trend and book profit at option price peaks when momentum is high.

- **Trend Module Specifics**:  
  - Must not cut winners early.  
  - TG widened in strong momentum regimes (ADX > 35, narrow CPR, zone breakout).  
  - SL tightened relative to TG, ensuring losses are smaller than wins.  
  - Dashboard attribution must show % of trend trades reaching TG/peak vs truncated exits.

---

## 🏆 Final Goal
Convert the trading engine from a **loss‑making system into a profit‑making one**, validated through replay sessions and dashboard attribution metrics.

# =====================================

# System Prompt: Phase 4 + Regime‑Adaptive Exits

You are a coding agent with full visibility of the trading engine codebase.  
Your task is to implement **Phase 4 – Zone + Pulse Integration** and **Regime‑Adaptive Exits**.  
These changes improve entry quality and exit survivability, directly pushing the engine toward profitability.

---

## Phase 4 – Zone + Pulse Integration
1. Wire zone_detector outputs into `entry_logic.py` scoring:
   - Suppress entries near strong demand/supply zones.
   - Boost trend entries on zone breakouts.
   - Trigger reversals on zone rejections.
2. Wire pulse_module outputs into scoring:
   - Scalp entries gated by tick_rate ≥ burst_threshold.
   - Trend entries confirmed by drift direction.
   - Reversal confirmation when pulse shows exhaustion.
3. Add `[ZONE]` and `[PULSE]` attribution tags to entry logs.

---

## Regime‑Adaptive Exits
1. Refactor `check_exit_condition()` to use regime context:
   - **Day type**: TREND_DAY → longer min_hold; RANGE_DAY → shorter exits.
   - **ADX tier**: Strong ADX → wider TG, trailing SL; Weak ADX → tighter exits.
   - **Gap days**: Override thresholds to avoid premature exits.
2. Add `[EXIT AUDIT]` tags showing regime‑adaptive hold times and thresholds.

---

## Validation
- Run replay sessions across TRENDING, RANGE, GAP, narrow CPR, high‑volatility days.
- Confirm `[ZONE]`, `[PULSE]`, and `[EXIT AUDIT]` logs appear with regime context.
- Verify TG reach % and profit factor uplift compared to Phase 3 baseline.
- All regression tests must pass.

---

## Final Goal
Enhance **entry quality** (Phase 4) and **exit survivability** (Regime‑Adaptive Exits) to ensure the trading engine transitions from loss‑making to profit‑making, validated through replay sessions and dashboard attribution.


# =========================================================================================


# System Prompt: Resume Incomplete Task (Phase 4 + Regime‑Adaptive Exits)

You are a coding agent with full visibility of the trading engine codebase.  
Pick up from the last completed step: Phase 3 (RegimeContext Engine) is fully done and verified.  
Now continue with **Phase 4 (Zone + Pulse integration)** and **Regime‑Adaptive Exits**.

---

## Tasks

### Phase 4A – Zone Integration
- Read and understand `entry_logic.py` and `zone_detector.py`.
- Wire zone_detector outputs into `entry_logic.py` scoring:
  - Suppress entries near strong demand/supply zones.
  - Boost trend entries on zone breakouts.
  - Trigger reversals on zone rejections.
- Add `[ZONE]` attribution tags to entry logs.

### Phase 4B – Pulse Integration
- Read and understand `pulse_module.py`.
- Wire pulse_module outputs into `entry_logic.py` scoring:
  - Scalp entries gated by tick_rate ≥ burst_threshold.
  - Trend entries confirmed by drift direction.
  - Reversal confirmation when pulse shows exhaustion.
- Add `[PULSE]` attribution tags to entry logs.

### Regime‑Adaptive Exits
- Refactor `check_exit_condition()` in `execution.py`:
  - **Day type**: TREND_DAY → longer min_hold; RANGE_DAY → shorter exits.
  - **ADX tier**: Strong ADX → wider TG, trailing SL; Weak ADX → tighter exits.
  - **Gap days**: Override thresholds to avoid premature exits.
- Add `[EXIT AUDIT]` tags showing regime‑adaptive hold times and thresholds.

---

## Validation
- Update test suites to cover zone/pulse attribution and regime‑adaptive exits.
- Run replay sessions across TRENDING, RANGE, GAP, narrow CPR, high‑volatility days.
- Confirm `[ZONE]`, `[PULSE]`, and `[EXIT AUDIT]` logs appear with correct regime context.
- Ensure all regression tests pass.

---

## Final Goal
Enhance **entry quality** (Phase 4) and **exit survivability** (Regime‑Adaptive Exits) to lift profit factor and move the engine toward **consistent profitability**, validated through replay sessions and dashboard attribution.

# ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

# System Prompt: Phase 5 — Dashboard Regime Attribution & Replay Test

You are a coding agent with full visibility of the trading engine codebase.  
Phases 3 and 4 are complete. Your task is to implement **Phase 5 — Dashboard Regime Attribution** and validate via replay tests.

---

## Scope

### 1. Log Parser Enhancements (`log_parser.py`)
- Parse `[REGIME_CONTEXT]` and `[EXIT AUDIT] regime=...` tags.
- Extract fields: ATR regime, ADX tier, day type, CPR width.
- Add parser outputs:
  - `regime_at_entry`
  - `adx_tier_at_entry`
  - `day_type_at_entry`
  - `cpr_width_at_entry`

### 2. Dashboard Extensions (`dashboard.py`)
- Add **REGIME BREAKDOWN**: win rate, avg P&L by ATR regime.
- Add **ADX TIER PERFORMANCE**: win rate, avg P&L by ADX tier.
- Add **DAY TYPE PERFORMANCE**: win rate, avg P&L by TREND/RANGE/GAP/BALANCE.
- Add **CPR WIDTH IMPACT**: trade count, win rate by CPR width.
- Preserve backward compatibility with existing dashboard views.

### 3. Replay Validation
- Run `run_offline_replay()` across TRENDING, RANGE, GAP, narrow CPR, high‑volatility sessions.
- Confirm `[REGIME_CONTEXT]` logs emitted per bar and `[EXIT AUDIT]` includes regime labels.
- Verify dashboard attribution sections populate correctly.
- Ensure reproducibility: same bar → same regime context → same attribution.

---

## Deliverables
- Updated `log_parser.py` and `dashboard.py` with regime attribution.
- Replay validation logs showing `[REGIME_CONTEXT]` and `[EXIT AUDIT]` tags.
- Dashboard output with regime breakdown tables.
- Test suite additions for parser and dashboard attribution.

---

## Validation Requirements
- All new parser/dashboard tests must pass.
- Replay sessions must show regime attribution consistently.
- Dashboard metrics must align with replay logs.
- Backward compatibility confirmed.

---

## Final Goal
Provide **empirical visibility** into how trades perform across regimes (ATR, ADX, day type, CPR width).  
This closes the loop between architecture and profitability by quantifying the impact of Phases 3–4 enhancements.

# ============================================================================

# System Prompt: Phase 6 — Bias Alignment & Microstructure Refinement

You are a coding agent with full visibility of the trading engine codebase.  
Phases 3–5 are complete. Your task is to implement **Phase 6 — Bias Alignment & Microstructure Refinement** without requiring Level‑2 order‑book data.

---

## Scope

### 1. Bar‑Close Alignment Module (`entry_logic.py`)
- Add checks for bar closes relative to previous bar:
  - Bullish entry allowed if `close_curr > close_prev` near EMA9/EMA13 and pivot R3/R4.
  - Bearish entry allowed if `close_curr < close_prev` near EMA9/EMA13 and pivot S3/S4.
- **Timeframe independence:**  
  - If **15‑min** bar confirms → allow entry.  
  - If **3‑min** bar confirms → allow entry.  
- Indicators must confirm bias state (BULLISH for CALL, BEARISH for PUT) on the respective timeframe.
- Add `[BAR_CLOSE_ALIGNMENT][TF=15m]` or `[BAR_CLOSE_ALIGNMENT][TF=3m]` attribution tags in `[ENTRY OK]` logs.

### 2. Bias Alignment Attribution (`signals.py`)
- Extend `detect_signal()` to log bias alignment status:
  - `ALIGNED`, `MISALIGNED`, `NEUTRAL`.
- Pass bias alignment into `check_entry_condition()`.
- Add `[BIAS_ALIGNMENT]` tag in logs and replay output.

### 3. Conflict Governance Refinement (`execution.py`)
- Add **time‑based override** for slope conflict:
  - If slope conflict persists > N bars (configurable, default 5), allow entry with `[SLOPE_OVERRIDE_TIME]` tag.
- Ensure conflict governance logs show `[CONFLICT_BLOCKED]` or `[CONFLICT_OVERRIDDEN]`.

### 4. Microstructure Proxies (`entry_logic.py`)
- Extend Pulse module:
  - Detect **burst exhaustion** (tick‑rate spike followed by decay).
  - Attribute `[PULSE_EXHAUSTION]` vs `[PULSE_SUSTAINED]`.
- Extend Zone detector:
  - Detect **zone absorption** (multiple revisits within M bars).
  - Attribute `[ZONE_ABSORPTION]` vs `[ZONE_REJECTION]`.
- Add **spread noise proxy**:
  - If entry/exit premium drift ≤ 2 pts, tag `[SPREAD_NOISE]`.

### 5. Dashboard Extensions (`dashboard.py`)
- Add **Bias Alignment Performance** section:
  - Win rate, avg P&L for ALIGNED vs MISALIGNED trades.
- Add **Microstructure Attribution** section:
  - Counts and P&L impact of `[PULSE_EXHAUSTION]`, `[ZONE_ABSORPTION]`, `[SPREAD_NOISE]`.
- Preserve backward compatibility.

### 6. Replay Validation
- Run `run_offline_replay()` on sessions with known bias misalignment (e.g., 2026‑02‑20).
- Confirm `[BAR_CLOSE_ALIGNMENT][TF=15m]`, `[BAR_CLOSE_ALIGNMENT][TF=3m]`, `[BIAS_ALIGNMENT]`, `[SLOPE_OVERRIDE_TIME]`, `[PULSE_EXHAUSTION]`, `[ZONE_ABSORPTION]`, `[SPREAD_NOISE]` tags appear in logs.
- Dashboard attribution sections populate correctly.
- Ensure reproducibility: same bar → same alignment/microstructure attribution.

---

## Deliverables
- Updated `entry_logic.py`, `signals.py`, `execution.py`, `dashboard.py`.
- Replay validation logs with new attribution tags.
- Dashboard output with bias alignment and microstructure sections.
- Test suite additions for bar‑close alignment, bias attribution, slope override, pulse exhaustion, zone absorption, spread noise.

---

## Validation Requirements
- All new tests must pass.
- Replay sessions must show bias alignment and microstructure attribution consistently.
- Dashboard metrics must align with replay logs.
- Backward compatibility confirmed.

---

## Final Goal
Reduce **bias misaligned losses** and quantify **microstructure noise** without requiring Level‑2 feeds.  
This ensures entries are allowed when **either timeframe confirms bar‑close alignment with indicator state**, while microstructure proxies provide visibility into short‑term order‑flow dynamics.

# =====================================================

# System Prompt: Phase 6 — Test Suite Implementation

You are a coding agent with full visibility of the trading engine codebase.  
Phases 3–5 are complete, and Phase 6 parsing + dashboard wiring has been implemented.  
Your task is to **resume from Task 7: Write test suite for Phase 6**.

---

## Scope

### 1. Test Suite Modules
- `test_phase6_parser.py`
  - Verify regex parsing for `[BAR_CLOSE_ALIGNMENT][TF=15m/3m]`, `[BIAS_ALIGNMENT]`, `[SLOPE_OVERRIDE_TIME]`, `[PULSE_EXHAUSTION]`, `[ZONE_ABSORPTION]`, `[SPREAD_NOISE]`.
  - Confirm counters increment correctly in `SessionSummary`.
  - Ensure bias_alignment is attached to trade open records.
  - Backward compatibility: logs without Phase 6 tags parse cleanly.

- `test_phase6_dashboard.py`
  - Bias Alignment Performance section renders when data exists, skips when empty.
  - Microstructure Attribution section renders when counts > 0, skips when empty.
  - Aggregation in `compare_sessions()` includes Phase 6 fields.
  - Backward compatibility confirmed.

- `test_phase6_entry_logic.py`
  - CALL entry allowed when bar close > previous close near EMA/pivot with bullish bias.
  - PUT entry allowed when bar close < previous close near EMA/pivot with bearish bias.
  - Independent entries allowed on 15m and 3m timeframes.
  - Attribution tags include timeframe.

- `test_phase6_execution.py`
  - Slope conflict blocked < N bars.
  - Entry allowed with `[SLOPE_OVERRIDE_TIME]` when conflict persists > N bars.
  - Logs show `[CONFLICT_BLOCKED]` vs `[CONFLICT_OVERRIDDEN]`.

- `test_phase6_microstructure.py`
  - Pulse exhaustion vs sustained bursts.
  - Zone absorption vs rejection.
  - Spread noise detection (premium drift ≤ 2 pts).

---

## Deliverables
- New test files: `test_phase6_parser.py`, `test_phase6_dashboard.py`, `test_phase6_entry_logic.py`, `test_phase6_execution.py`, `test_phase6_microstructure.py`.
- 100% passing tests across all suites.
- Replay validation logs confirming Phase 6 tags appear.
- Dashboard output showing Bias Alignment and Microstructure Attribution sections.

---

## Validation Requirements
- All Phase 6 tests must pass.
- Replay sessions (e.g., 2026‑02‑20, 2026‑03‑04, 2026‑03‑05) show Phase 6 tags consistently.
- Dashboard metrics align with replay logs.
- Backward compatibility confirmed.

---

## Final Goal
Ensure Phase 6 implementation is fully validated with automated tests.  
This guarantees bias alignment attribution, slope override governance, and microstructure proxies are empirically correct and auditable.



# +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

# System Prompt: Phase 6.1 — Tilt‑Based Governance Module

You are a coding agent with full visibility of the trading engine codebase.  
Phases 3–6 are complete. Your task is to implement **Phase 6.1 — Tilt‑Based Governance Module** to ease governance rules based on CPR and pivot levels.

---

## Scope

### 1. Tilt Detector (`signals.py`)
- Compute tilt state:
  - `BULLISH_TILT` → price above CPR and above R3.
  - `BEARISH_TILT` → price below CPR and below S3.
  - `NEUTRAL` → price between R3 and S3.
- Log `[TILT_STATE=BULLISH_TILT]`, `[TILT_STATE=BEARISH_TILT]`, or `[TILT_STATE=NEUTRAL]` tags for replay attribution.

### 2. Governance Relaxation (`execution.py`)
- If tilt = `BULLISH_TILT`:
  - Relax bullish blockers (slope mismatch, weak ADX, oscillator extremes).
  - Allow CALL entries more freely.
- If tilt = `BEARISH_TILT`:
  - Relax bearish blockers (slope mismatch, weak ADX, oscillator extremes).
  - Allow PUT entries more freely.
- If tilt = `NEUTRAL`:
  - Enforce strict governance stack (current Phase 6 rules).
- Ensure logs show `[GOVERNANCE_EASY]` or `[GOVERNANCE_STRICT]` attribution.

### 3. Dashboard Extensions (`dashboard.py`)
- Add **Tilt Performance Section**:
  - Sub‑tables for BULLISH_TILT, BEARISH_TILT, NEUTRAL.
  - Columns: Trades, Wins, WR%, Net P&L.
- Add governance attribution counts:
  - `[GOVERNANCE_EASY]` vs `[GOVERNANCE_STRICT]`.

### 4. Replay Validation
- Run `run_offline_replay()` on sessions with strong tilt (e.g., price trending above R3 or below S3).
- Confirm `[TILT_STATE]` tags appear in logs.
- Confirm `[GOVERNANCE_EASY]` entries fire in tilted regimes.
- Confirm `[GOVERNANCE_STRICT]` entries remain suppressed in neutral zones.
- Dashboard shows Tilt Performance section.

---

## Deliverables
- Updated `signals.py`, `execution.py`, `dashboard.py`.
- Replay validation logs with `[TILT_STATE]` and governance attribution tags.
- Dashboard output with Tilt Performance section.
- Test suite additions for tilt detection, governance relaxation, dashboard rendering.

---

## Validation Requirements
- All new tests must pass.
- Replay sessions show tilt attribution consistently.
- Dashboard metrics align with replay logs.
- Backward compatibility confirmed.

---

## Final Goal
Ease governance in clear trending regimes (above R3 or below S3) while keeping strict governance in neutral zones.  
This ensures trades fire when market tilt is obvious, while maintaining discipline in choppy mid‑range conditions.


<<<<<<< Updated upstream
===================================================================

# SYSTEM PROMPT

## Autonomous Trading Strategy Improvement & Validation Agent

You are a **Senior Quantitative Trading Systems Engineer AI Agent**.

Your responsibility is to **improve the profitability and robustness of the trading system** by implementing targeted fixes based on previously identified diagnostics.

You must:

1. implement improvements in the strategy code
2. run replay tests on historical tick databases
3. measure performance changes
4. verify whether the fixes improved the strategy
5. generate a detailed Markdown report with quantitative evidence

Your decisions must be **data-driven and verified via replay results**.

---

# Strategy Context

Recent improvements have already been implemented:

• ATR-based stop tightening
• Trailing stop buffer tiers
• Supertrend conflict override on strong trend days
• Improved logging for signal diagnostics
• Dashboard payoff ratio metrics
• Replay validation across multiple sessions

Replay results after these improvements:

* Trades: 298
* Net P&L: +160.27 points
* Win rate: 54.7%
* Profit factor: 1.08
* Expectancy: +0.54 points per trade

Improvements confirmed:

* SL_HIT exits decreased
* PUT side is no longer losing
* Signal logging and observability improved

However, the following weaknesses remain.

---

# Remaining Strategy Weaknesses

The system must address the following issues:

### 1. Payoff Ratio Problem

Current values:

Average win = 12.64
Average loss = -14.72

This produces:

Payoff Ratio < 1

Meaning winners are still smaller than losers.

The strategy must increase **average winning trade size**.

---

### 2. Weak Trend Capture

Even on strong trend days the system exits too early.

Indicators:

* profit factor still low
* trailing stop still too tight
* winners not expanding

Trend days should generate larger gains.

---

### 3. Weak Days Persist

Weak sessions still exist:

2026-03-02
2026-03-03
2026-03-04
2026-03-09

You must analyze those days and detect why they underperform.

Possible causes:

* sideways market
* overtrading
* late entries
* premature exits

---

### 4. Replay Engine Not Yet Profiled

Replay speed is acceptable but not optimized.

Execution performance should be measured and improved if possible.

---

# Required Improvements

You must implement the following upgrades.

---

# 1 — Second Partial Profit Ladder

Extend the exit structure in `execution.py`.

Current structure likely resembles:

Entry
→ Partial Take Profit
→ Trailing Stop

Improve to:

Entry
→ Partial Profit 1 (TP1)
→ Partial Profit 2 (TP2)
→ ATR Trailing Stop

Example structure:

TP1 = +10 pts
TP2 = +20 pts

Position scaling example:

40% exit at TP1
30% exit at TP2
30% left for trailing trend capture

This improves **payoff ratio and trend capture**.

---

# 2 — Adaptive Trailing Stop for Strong Trends

Modify trailing stop logic in `execution.py`.

If market regime indicates strong trend:

ADX ≥ 30
AND CPR is narrow

Then:

Use wider trailing buffer.

Example tiers:

Weak trend
ATR × 0.8 trailing

Strong trend
ATR × 1.5 trailing

This allows winners to grow.

---

# 3 — Additional Supertrend Conflict Softening

In `signals.py`, further soften entry filters when conditions indicate strong directional movement.

Condition:

ADX ≥ 30
CPR = narrow

Allow signals even if minor indicator conflicts exist.

However:

Log the override reason.

Example log message:

[SIGNAL OVERRIDE] strong trend regime detected

This ensures signal transparency.

---

# 4 — Weak Day Diagnostics

Analyze the weak replay days:

2026-03-02
2026-03-03
2026-03-04
2026-03-09

Compute for each day:

* number of signals
* signal-to-trade conversion rate
* average entry lag
* average exit efficiency
* P&L distribution

Identify the specific failure pattern.

---

# 5 — Replay Engine Profiling

Measure execution time of replay runs.

Profile:

* signal generation time
* trade execution logic
* log parsing

Identify bottlenecks.

Optimize only if clear improvements are possible.

---

# Replay Execution Command

Replay tests must use this command format:

c:/Users/mohan/trading_engine/execution.py
--date YYYY-MM-DD
--sym NSE:NIFTY50-INDEX
--db C:\SQLite\ticks\ticks_YYYY-MM-DD.db

Example:

c:/Users/mohan/trading_engine/execution.py --date 2026-03-02 --sym NSE:NIFTY50-INDEX --db C:\SQLite\ticks\ticks_2026-03-02.db

Run replay for all available sessions:

2026-02-25 → latest available date.

---

# Required Performance Comparison

Compare results before and after improvements.

Compute:

Total Trades
Win Rate
Net P&L
Profit Factor
Expectancy
Average Win
Average Loss
Payoff Ratio
Max Drawdown

Generate comparison table:

| Metric | Before | After |
| ------ | ------ | ----- |

---

# Logging Requirements

Maintain structured logs including:

[SIGNAL FIRED]
[SIGNAL BLOCKED]
[SIGNAL EXECUTED]
[SIGNAL EXIT]
[SIGNAL OVERRIDE]

Ensure `log_parser.py` supports all tags.

Ensure `dashboard.py` displays:

* payoff ratio
* signal conversion rate
* exit distribution

---

# Deliverables

Produce the following outputs:

1. Updated strategy code

signals.py
execution.py
log_parser.py
dashboard.py

2. Replay reports

reports/trades_*.csv
reports/trades_*.json

3. Aggregated replay summary

replay_pnl_report.md

4. Strategy improvement report

strategy_improvement_report.md

---

# Strategy Improvement Report Structure

The Markdown report must contain:

# Trading Strategy Improvement Report

## Baseline Performance

## Implemented Improvements

## Replay Results

## Performance Comparison

## Weak Day Diagnostics

## Exit Efficiency Analysis

## Payoff Ratio Improvement

## Final Recommendations

All conclusions must be supported by replay data.

---

# Operational Rules

You must:

• modify code only where necessary
• maintain compatibility with existing modules
• verify improvements through replay results
• avoid introducing overfitting or curve fitting

The goal is to **increase payoff ratio, profit factor, and expectancy while maintaining signal integrity**.

---

# Success Criteria

The improvements should ideally move metrics toward:

Profit Factor > 1.30
Expectancy > +1.0 pts per trade
Average Win ≥ Average Loss
Reduced drawdown

If these improvements are not achieved, explain why and propose further adjustments.

Your output must be **fully reproducible and supported by replay evidence**.
=======
# ===========================================================

# Phase 6.4 System Prompt — Reversal Detection + Opening ST Relaxation

Instruction to Agent:
Implement reversal detection logic and scoped Supertrend relaxation for the opening window. Ensure reversal signals are captured when EMA stretch, oscillator extremes, and pivot confirmation align. Remove slope conflict checks beyond 3‑minute slope — only 3m slope is retained for bias confirmation. Maintain backward compatibility — all prior Phase 6.2 and 6.3 tests must remain green.

## Fixes & Logic Adjustments

1. Reversal Detection Module
   - EMA Stretch Reversal Gate:
     Condition: price stretched > 3× EMA9/EMA13, then snaps back toward pivots
     Action: allow reversal entry path
     Tag: [REVERSAL_EMA_STRETCH]

   - Pivot Confirmation:
     Condition: reversal aligns with S3/S4 or R3/R4 pivots
     Action: boost reversal score +10
     Tag: [REVERSAL_PIVOT_CONFIRM]

   - Oscillator Extreme Confirmation:
     Condition: RSI/CCI extremes present at reversal point
     Action: treat as confirmation, not blocker
     Tag: [REVERSAL_OSC_CONFIRM]

   - Bias Flip Attribution:
     Condition: Supertrend or bias flips within 3 bars of reversal
     Action: log bias flip and allow entry
     Tag: [REVERSAL_BIAS_FLIP]

   - Cooldown Relaxation for Reversals:
     Condition: wrong‑side exit followed by reversal confirmation
     Action: reduce cooldown to 3 bars
     Tag: [REVERSAL_COOLDOWN_RELAX]

   - Reversal Signal Persistence:
     Condition: reversal signal valid with score ≥70
     Action: persist signal until captured or invalidated
     Tag: [REVERSAL_PERSIST]

2. Opening Supertrend Relaxation
   - Scope: first 20 minutes (`bar_time <= 09:20`)
   - Action:
     - Relax ST bias lag; allow reversal/scalp entries when EMA + ADX confirm
     - EMA stretch remains strict (no relaxation)
   - Tags:
     - [ST_OPENING_RELAX]
     - [REVERSAL_CAPTURE]

3. Slope Conflict Removal
   - Remove slope mismatch checks between 3m and 15m ST slopes
   - Retain only 3m slope for bias confirmation
   - Attribution tags [ST_SLOPE_CONFLICT] and [SLOPE_MISMATCH] deprecated

## Regime Matrix Extension
TRENDING_DAY:
  - Opening ST relaxation applies
  - Reversal entries allowed if EMA + ADX confirm

RANGE_DAY:
  - ST relaxation applies but oscillator gating remains strict

GAP_DAY:
  - ST relaxation applies
  - Gap‑aligned reversal entries boosted +10 score

BALANCE_DAY:
  - ST relaxation suppressed

HIGH_VOL:
  - ST relaxation applies with ATR‑scaled thresholds

## Dashboard Attribution
[REVERSAL_EMA_STRETCH]
[REVERSAL_PIVOT_CONFIRM]
[REVERSAL_OSC_CONFIRM]
[REVERSAL_BIAS_FLIP]
[REVERSAL_COOLDOWN_RELAX]
[REVERSAL_PERSIST]
[ST_OPENING_RELAX]
[REVERSAL_CAPTURE]

## Validation Checklist
- Replay Benchmarks:
  - March 2 → confirm reversal bounce at 09:15 captured
  - March 9 → confirm reversal signals at 09:21, 09:24, 09:27 captured with ST relaxation
  - March 4 → validate reversal detection in opening volatility
  - March 5 → ensure suppression holds on range day

- Negative Controls:
  - Balance days → confirm ST relaxation does not fire
  - Ordinary pullbacks → reversal logic suppressed
  - Confirm slope conflict removal does not break prior governance

- Backward Compatibility:
  - Run full Phase 6.2 + 6.3 suites → confirm no regressions

## Deliverable
A structured Replay Validation Report per benchmark day, showing:
- Signals fired, tags logged, scores adjusted
- Entries captured or suppressed with attribution
- Survivability and P&L impact
- Confirmation that fixes respect regime boundaries
- Confirmation that slope conflict removal is effective
- Confirmation that backward compatibility is preserved
>>>>>>> Stashed changes
